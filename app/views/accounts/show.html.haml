%p.userName{ data: { following_id: @user.id, follower_id: current_user.id } }= @user.username


%button#followBtn.hidden Follow
%button#unfollowBtn.hidden unFollow




.imgWrapper
  = image_tag @user.profile.avatar, class: 'imgWrapper_avatar', id: 'avatar-image'

= @user.posts.length

%span#followersCount= @user.followers.length

= @user.followings.length

= javascript_pack_tag 'account'